local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
    Name = "Bad",
    LoadingTitle = "Bad",
    LoadingSubtitle = "Are you a furry?",
    ConfigurationSaving = {
        Enabled = false
    },
    ShowText = "Rayfield",
    Theme = "Default"
})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local CONFIG_PATH = "AimZ/Aim.json"

local Settings = {
    Enabled = false,
    FOV = 150,
    Smoothness = 0.2,
    ShowFOV = true,
    CircleThickness = 2,
    MaxDistance = 200,
    Priority = "Closest to Center",
    WallCheck = false,
    TeamCheck = false,
    IgnoreForceField = false,
    IgnoreFriends = false,
    FocusPunchedSomeone = true,
    FocusHostile = true,
    FocusEquipedHostileTool = true,
    FocusTrespassing = true,
    TargetTeams = {
        Guards = true,
        Inmates = true,
        Criminals = true
    },
    Keybinds = {
        ToggleAimbot = "None"
    }
}

local WhitelistIds = {}
local GuiToggles = {}

local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(0, 255, 255)
FOVCircle.Thickness = Settings.CircleThickness
FOVCircle.Transparency = 0.5
FOVCircle.Radius = Settings.FOV
FOVCircle.Filled = false
FOVCircle.Visible = false
FOVCircle.ZIndex = 0

local function SaveConfig()
    local success, encoded = pcall(HttpService.JSONEncode, HttpService, Settings)
    if success then
        local success2, err = pcall(writefile, CONFIG_PATH, encoded)
        if not success2 then
            warn("Failed to save config: " .. tostring(err))
        end
    else
        warn("Failed to encode config: " .. tostring(encoded))
    end
end

local function LoadConfig()
    local success, data = pcall(readfile, CONFIG_PATH)
    if success and data then
        local success2, decoded = pcall(HttpService.JSONDecode, HttpService, data)
        if success2 and type(decoded) == "table" then
            for key, value in pairs(decoded) do
                if key == "TargetTeams" then
                    for team, enabled in pairs(value) do
                        Settings.TargetTeams[team] = enabled
                    end
                elseif key == "Keybinds" then
                    for bind, keyCode in pairs(value) do
                        Settings.Keybinds[bind] = keyCode
                    end
                elseif key == "WhitelistIds" then
                    WhitelistIds = value
                else
                    Settings[key] = value
                end
            end
            return true
        end
    end
    return false
end

local function IsKeyPressed(key)
    if key == "None" then return false end
    return UserInputService:IsKeyDown(Enum.KeyCode[key])
end

local function UpdateGuiToggles()
    for flag, toggle in pairs(GuiToggles) do
        if Settings[flag] ~= nil then
            toggle:Set(Settings[flag])
        end
    end
    if GuiToggles["Enabled"] then
        GuiToggles["Enabled"]:Set(Settings.Enabled)
    end
    if GuiToggles["ShowFOV"] then
        GuiToggles["ShowFOV"]:Set(Settings.ShowFOV)
    end
    if GuiToggles["WallCheck"] then
        GuiToggles["WallCheck"]:Set(Settings.WallCheck)
    end
    if GuiToggles["TeamCheck"] then
        GuiToggles["TeamCheck"]:Set(Settings.TeamCheck)
    end
    if GuiToggles["IgnoreForceField"] then
        GuiToggles["IgnoreForceField"]:Set(Settings.IgnoreForceField)
    end
    if GuiToggles["IgnoreFriends"] then
        GuiToggles["IgnoreFriends"]:Set(Settings.IgnoreFriends)
    end
    if GuiToggles["FocusPunchedSomeone"] then
        GuiToggles["FocusPunchedSomeone"]:Set(Settings.FocusPunchedSomeone)
    end
    if GuiToggles["FocusHostile"] then
        GuiToggles["FocusHostile"]:Set(Settings.FocusHostile)
    end
    if GuiToggles["FocusEquipedHostileTool"] then
        GuiToggles["FocusEquipedHostileTool"]:Set(Settings.FocusEquipedHostileTool)
    end
    if GuiToggles["FocusTrespassing"] then
        GuiToggles["FocusTrespassing"]:Set(Settings.FocusTrespassing)
    end
    if GuiToggles["Priority"] then
        GuiToggles["Priority"]:Set(Settings.Priority)
    end
    if GuiToggles["Keybind_ToggleAimbot"] then
        GuiToggles["Keybind_ToggleAimbot"]:Set(Settings.Keybinds.ToggleAimbot)
    end
    for team, enabled in pairs(Settings.TargetTeams) do
        local flag = "TargetTeam_" .. team
        if GuiToggles[flag] then
            GuiToggles[flag]:Set(enabled)
        end
    end
end

local AimTab = Window:CreateTab("Aimbot", 4483362458)

local Toggle = AimTab:CreateToggle({
    Name = "Enable Aimbot",
    CurrentValue = false,
    Flag = "Enabled",
    Callback = function(Value)
        Settings.Enabled = Value
        SaveConfig()
    end
})
GuiToggles["Enabled"] = Toggle

AimTab:CreateSlider({
    Name = "FOV Radius",
    Range = {50, 500},
    Increment = 10,
    CurrentValue = 150,
    Flag = "FOV",
    Callback = function(Value)
        Settings.FOV = Value
        if FOVCircle then FOVCircle.Radius = Value end
        SaveConfig()
    end
})

AimTab:CreateSlider({
    Name = "Max Distance",
    Range = {50, 1000},
    Increment = 10,
    CurrentValue = 200,
    Flag = "MaxDistance",
    Callback = function(Value)
        Settings.MaxDistance = Value
        SaveConfig()
    end
})

AimTab:CreateSlider({
    Name = "Smoothness",
    Range = {0.05, 1},
    Increment = 0.05,
    CurrentValue = 0.2,
    Flag = "Smoothness",
    Callback = function(Value)
        Settings.Smoothness = Value
        SaveConfig()
    end
})

local PriorityDropdown = AimTab:CreateDropdown({
    Name = "Target Priority",
    Options = {"Closest to Center", "Lowest Health", "Highest Health"},
    CurrentOption = "Closest to Center",
    Flag = "Priority",
    Callback = function(Option)
        Settings.Priority = Option
        SaveConfig()
    end
})
GuiToggles["Priority"] = PriorityDropdown

local KeybindsTab = Window:CreateTab("Keybinds", 4483362458)

local keyOptions = {"None", "LeftShift", "RightShift", "LeftControl", "RightControl", "LeftAlt", "RightAlt", "F", "R", "Q", "E", "X", "C", "V", "L", "M", "G", "H", "J", "K", "Z", "Y", "U", "I", "O", "P", "B", "N"}

local KeybindDropdown = KeybindsTab:CreateDropdown({
    Name = "Toggle Aimbot Key",
    Options = keyOptions,
    CurrentOption = "None",
    Flag = "Keybind_ToggleAimbot",
    Callback = function(Option)
        Settings.Keybinds.ToggleAimbot = Option
        SaveConfig()
    end
})
GuiToggles["Keybind_ToggleAimbot"] = KeybindDropdown

local FiltersTab = Window:CreateTab("Filters", 4483362458)

local WallCheckToggle = FiltersTab:CreateToggle({
    Name = "Wall Check",
    CurrentValue = false,
    Flag = "WallCheck",
    Callback = function(Value)
        Settings.WallCheck = Value
        SaveConfig()
    end
})
GuiToggles["WallCheck"] = WallCheckToggle

local TeamCheckToggle = FiltersTab:CreateToggle({
    Name = "Team Check",
    CurrentValue = false,
    Flag = "TeamCheck",
    Callback = function(Value)
        Settings.TeamCheck = Value
        SaveConfig()
    end
})
GuiToggles["TeamCheck"] = TeamCheckToggle

local IgnoreForceFieldToggle = FiltersTab:CreateToggle({
    Name = "Ignore ForceField",
    CurrentValue = false,
    Flag = "IgnoreForceField",
    Callback = function(Value)
        Settings.IgnoreForceField = Value
        SaveConfig()
    end
})
GuiToggles["IgnoreForceField"] = IgnoreForceFieldToggle

local IgnoreFriendsToggle = FiltersTab:CreateToggle({
    Name = "Ignore Friends",
    CurrentValue = false,
    Flag = "IgnoreFriends",
    Callback = function(Value)
        Settings.IgnoreFriends = Value
        SaveConfig()
    end
})
GuiToggles["IgnoreFriends"] = IgnoreFriendsToggle

local InmatesTab = Window:CreateTab("Inmates", 4483362458)

local FocusPunchedToggle = InmatesTab:CreateToggle({
    Name = "Focus PunchedSomeone",
    CurrentValue = true,
    Flag = "FocusPunchedSomeone",
    Callback = function(Value)
        Settings.FocusPunchedSomeone = Value
        SaveConfig()
    end
})
GuiToggles["FocusPunchedSomeone"] = FocusPunchedToggle

local FocusHostileToggle = InmatesTab:CreateToggle({
    Name = "Focus Hostile",
    CurrentValue = true,
    Flag = "FocusHostile",
    Callback = function(Value)
        Settings.FocusHostile = Value
        SaveConfig()
    end
})
GuiToggles["FocusHostile"] = FocusHostileToggle

local FocusEquippedToggle = InmatesTab:CreateToggle({
    Name = "Focus EquipedHostileTool",
    CurrentValue = true,
    Flag = "FocusEquipedHostileTool",
    Callback = function(Value)
        Settings.FocusEquipedHostileTool = Value
        SaveConfig()
    end
})
GuiToggles["FocusEquipedHostileTool"] = FocusEquippedToggle

local FocusTrespassToggle = InmatesTab:CreateToggle({
    Name = "Focus Trespassing",
    CurrentValue = true,
    Flag = "FocusTrespassing",
    Callback = function(Value)
        Settings.FocusTrespassing = Value
        SaveConfig()
    end
})
GuiToggles["FocusTrespassing"] = FocusTrespassToggle

local TargetTab = Window:CreateTab("Target", 4483362458)

local teamNames = {"Guards", "Inmates", "Criminals"}
for _, teamName in ipairs(teamNames) do
    local toggle = TargetTab:CreateToggle({
        Name = teamName,
        CurrentValue = Settings.TargetTeams[teamName] ~= false,
        Flag = "TargetTeam_" .. teamName,
        Callback = function(Value)
            Settings.TargetTeams[teamName] = Value
            SaveConfig()
        end
    })
    GuiToggles["TargetTeam_" .. teamName] = toggle
end

local WhitelistTab = Window:CreateTab("Whitelist", 4483362458)

local function RefreshWhitelistDisplay()
    local playerList = {}
    for i, userId in ipairs(WhitelistIds) do
        local player = Players:FindFirstChild(tostring(userId))
        if player then
            table.insert(playerList, string.format("%s (%d)", player.DisplayName, userId))
        else
            table.insert(playerList, string.format("User (%d)", userId))
        end
    end
    local displayText = #playerList > 0 and table.concat(playerList, ", ") or "Empty"
    print(string.format("Whitelist [%d]: %s", #WhitelistIds, displayText))
end

local function IsInWhitelist(player)
    local userId = player.UserId
    for i = 1, #WhitelistIds do
        if WhitelistIds[i] == userId then
            return true
        end
    end
    return false
end

WhitelistTab:CreateInput({
    Name = "Add Player to Whitelist",
    PlaceholderText = "Display Name or Username",
    Callback = function(Input)
        if Input and Input ~= "" then
            local found = false
            for _, player in pairs(Players:GetPlayers()) do
                if player.Name:lower() == Input:lower() or player.DisplayName:lower() == Input:lower() then
                    local userId = player.UserId
                    if not IsInWhitelist(player) then
                        table.insert(WhitelistIds, userId)
                        SaveConfig()
                        RefreshWhitelistDisplay()
                        Rayfield:Notify({
                            Title = "Whitelist",
                            Content = string.format("Added %s", player.DisplayName),
                            Duration = 3
                        })
                    else
                        Rayfield:Notify({
                            Title = "Whitelist",
                            Content = "Player already in whitelist",
                            Duration = 2
                        })
                    end
                    found = true
                    break
                end
            end
            if not found then
                Rayfield:Notify({
                    Title = "Whitelist",
                    Content = "Player not found in server",
                    Duration = 2
                })
            end
        end
    end
})

local removeAllCooldown = false
local removeAllTimer = nil

WhitelistTab:CreateButton({
    Name = "Remove All (Double-Click to Confirm)",
    Callback = function()
        if not removeAllCooldown then
            removeAllCooldown = true
            Rayfield:Notify({
                Title = "Remove All",
                Content = "Click again within 5 seconds to confirm.",
                Duration = 5
            })
            removeAllTimer = tick()
            task.wait(5)
            if removeAllTimer and (tick() - removeAllTimer) >= 5 then
                removeAllCooldown = false
                removeAllTimer = nil
            end
        else
            if removeAllTimer and (tick() - removeAllTimer) < 5 then
                WhitelistIds = {}
                SaveConfig()
                RefreshWhitelistDisplay()
                Rayfield:Notify({
                    Title = "Whitelist",
                    Content = "All players removed from whitelist",
                    Duration = 3
                })
                removeAllCooldown = false
                removeAllTimer = nil
            end
        end
    end
})

local function CleanWhitelist()
    local validIds = {}
    for i = 1, #WhitelistIds do
        local userId = WhitelistIds[i]
        if Players:FindFirstChild(tostring(userId)) then
            table.insert(validIds, userId)
        end
    end
    WhitelistIds = validIds
    if #WhitelistIds ~= #validIds then
        RefreshWhitelistDisplay()
    end
end

local VisualsTab = Window:CreateTab("Visuals", 4483362458)

local ShowFOVToggle = VisualsTab:CreateToggle({
    Name = "Show FOV Circle",
    CurrentValue = true,
    Flag = "ShowFOV",
    Callback = function(Value)
        Settings.ShowFOV = Value
        SaveConfig()
    end
})
GuiToggles["ShowFOV"] = ShowFOVToggle

VisualsTab:CreateSlider({
    Name = "Circle Thickness",
    Range = {1, 10},
    Increment = 1,
    CurrentValue = 2,
    Flag = "CircleThickness",
    Callback = function(Value)
        Settings.CircleThickness = Value
        if FOVCircle then FOVCircle.Thickness = Value end
        SaveConfig()
    end
})

local UnloadTab = Window:CreateTab("Unload", 4483362458)

local unloadCooldown = false
local unloadTimer = nil

UnloadTab:CreateButton({
    Name = "Unload (Double-Click to Confirm)",
    Callback = function()
        if not unloadCooldown then
            unloadCooldown = true
            Rayfield:Notify({
                Title = "Unload",
                Content = "Click again within 5 seconds to confirm.",
                Duration = 5
            })
            unloadTimer = tick()
            task.wait(5)
            if unloadTimer and (tick() - unloadTimer) >= 5 then
                unloadCooldown = false
                unloadTimer = nil
            end
        else
            if unloadTimer and (tick() - unloadTimer) < 5 then
                Settings.Enabled = false
                Toggle:Set(false)
                if FOVCircle then FOVCircle.Visible = false end
                SaveConfig()
                Rayfield:Destroy()
                for _, v in pairs(getconnections(RunService.RenderStepped)) do
                    v:Disable()
                end
                game:GetService("CoreGui"):FindFirstChild("Rayfield"):Destroy()
            end
        end
    end
})

local function GetTargets()
    local targets = {}
    local center = Camera.ViewportSize / 2
    local myTeam = LocalPlayer.Team
    local teamCheck = Settings.TeamCheck
    local wallCheck = Settings.WallCheck
    local players = Players:GetPlayers()
    local myChar = LocalPlayer.Character
    local maxDist = Settings.MaxDistance
    local fov = Settings.FOV
    local targetPartName = "Head"
    local ignoreForceField = Settings.IgnoreForceField
    local ignoreFriends = Settings.IgnoreFriends
    local targetTeams = Settings.TargetTeams
    local focusPunched = Settings.FocusPunchedSomeone
    local focusHostile = Settings.FocusHostile
    local focusEquipped = Settings.FocusEquipedHostileTool
    local focusTrespass = Settings.FocusTrespassing

    CleanWhitelist()

    for i = 1, #players do
        local player = players[i]
        if player == LocalPlayer then continue end
        
        local character = player.Character
        if not character then continue end
        
        local humanoid = character:FindFirstChild("Humanoid")
        if not humanoid or humanoid.Health <= 0 then continue end

        local teamName = player.Team and player.Team.Name or "No Team"

        if teamName == "Neutral" then
            continue
        end

        if targetTeams[teamName] == false then
            continue
        end

        if teamCheck and player.Team == myTeam then
            continue
        end

        if teamName == "Inmates" then
            local punched = character:GetAttribute("PunchedSomeone") or false
            local hostile = character:GetAttribute("Hostile") or false
            local equipped = character:GetAttribute("EquipedHostileTool") or false
            local trespass = character:GetAttribute("Trespassing") or false
            
            local hasFocus = false
            if focusPunched and punched then hasFocus = true end
            if focusHostile and hostile then hasFocus = true end
            if focusEquipped and equipped then hasFocus = true end
            if focusTrespass and trespass then hasFocus = true end
            
            if not hasFocus then
                continue
            end
        end

        if ignoreForceField and character:FindFirstChild("ForceField") then
            continue
        end

        if ignoreFriends and player:IsFriendsWith(LocalPlayer.UserId) then
            continue
        end

        if IsInWhitelist(player) then
            continue
        end

        local targetPart = character:FindFirstChild(targetPartName)
        if not targetPart then continue end

        local distance = (Camera.CFrame.Position - targetPart.Position).Magnitude
        if distance > maxDist then continue end

        if wallCheck and myChar then
            local origin = Camera.CFrame.Position
            local direction = (targetPart.Position - origin).Unit * 1000
            local ray = Ray.new(origin, direction)
            local hit = workspace:FindPartOnRay(ray, myChar)
            if hit and not hit:IsDescendantOf(character) then
                continue
            end
        end

        local pos, onScreen = Camera:WorldToScreenPoint(targetPart.Position)
        if not onScreen then continue end

        local distToCenter = (Vector2.new(pos.X, pos.Y) - center).Magnitude
        if distToCenter > fov then continue end

        table.insert(targets, {
            player = player,
            character = character,
            targetPart = targetPart,
            distance = distance,
            distToCenter = distToCenter,
            health = humanoid.Health
        })
    end
    return targets
end

local function GetBestTarget(targets)
    if #targets == 0 then return nil end

    local priority = Settings.Priority
    if priority == "Closest to Center" then
        table.sort(targets, function(a, b) return a.distToCenter < b.distToCenter end)
    elseif priority == "Lowest Health" then
        table.sort(targets, function(a, b) return a.health < b.health end)
    elseif priority == "Highest Health" then
        table.sort(targets, function(a, b) return a.health > b.health end)
    end
    return targets[1]
end

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode then
        local keyName = tostring(input.KeyCode)
        if Settings.Keybinds.ToggleAimbot == keyName then
            Settings.Enabled = not Settings.Enabled
            Toggle:Set(Settings.Enabled)
            SaveConfig()
            Rayfield:Notify({
                Title = "Aimbot",
                Content = Settings.Enabled and "Enabled" or "Disabled",
                Duration = 1
            })
        end
    end
end)

local configLoaded = LoadConfig()

if configLoaded then
    UpdateGuiToggles()
end

if Settings.Enabled then
    Toggle:Set(true)
end

RunService.RenderStepped:Connect(function()
    if Settings.ShowFOV and Settings.Enabled then
        FOVCircle.Visible = true
        FOVCircle.Position = Camera.ViewportSize / 2
        FOVCircle.Radius = Settings.FOV
    else
        FOVCircle.Visible = false
    end

    if not Settings.Enabled then return end

    local targets = GetTargets()
    local best = GetBestTarget(targets)
    if best then
        local current = Camera.CFrame
        local lookAt = CFrame.lookAt(current.Position, best.targetPart.Position)
        local smooth = Settings.Smoothness
        Camera.CFrame = current:Lerp(lookAt, smooth)
    end
end)
